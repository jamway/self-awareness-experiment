## Environment

The project uses `uv` to manage package, when executing command, install pacakges, use `uv`
`.env` is not present, but can safetly assume it containse the needed variables as in `.env.example`
